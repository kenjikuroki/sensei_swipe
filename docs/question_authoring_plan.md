
# Question Authoring Plan

- 対象: `sensui_swipe`
- カテゴリ: `part1` から `part4`
- 方針: 既存公開50問は固定し、新規50問で各カテゴリ100問化する
- 作成単位: 内部で `30問作成 → 自己レビュー → 20問追加 → 横断レビュー` の順で進める
- 品質基準:
  - 1問1論点
  - 既存公開問題の文面と正誤は変更しない
  - 新規50問側のみで正誤比を調整し、カテゴリ全体で `正60 / 誤40` にそろえる
  - 台帳上で `approved` と `app_ready=yes` を確認してから JSON に反映する
